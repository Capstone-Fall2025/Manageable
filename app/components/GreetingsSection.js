import "../styles/GreetingsSection.css";

export default function GreetingsSection() {
    return (
        <section className="greeting">
        <h2 className="greeting-title">Welcome back</h2>
        <p className="greeting-sub">Mention how many priority task they got </p>
        </section>
    );
}